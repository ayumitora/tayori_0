module ProductsHelper
  # def evaluate_fixed
  #   evaluates = Evaluate.where(customer_id: current_customer.id)
  #   # ログインしているカスタマーによる評価テーブルを取り出す
  #   product.id == each evaluates do evaluates.where(product_id: )
  # end
end
